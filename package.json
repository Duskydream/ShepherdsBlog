{
  "name": "starlight",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "check": "astro check",
    "prebuild": "node ./scripts/fetch-bangumi.mjs",
    "build": "astro build",
    "build:functions": "tsc -p tsconfig.functions.json",
    "deploy": "node ./scripts/clean-astro.mjs && pnpm run build && pnpm run build:functions && node ./scripts/deploy.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/internal-helpers": "0.8.0",
    "@astrojs/starlight": "^0.38.3",
    "astro": "^6.1.5",
    "sharp": "^0.34.2",
    "starlight-theme-rapide": "^0.5.2"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
